# 交接說明

給接手這份程式碼的人。**先讀這一份，再讀 `CLAUDE.md`**（那裡是動手改的時候要遵守的架構規則）。

---

## 一、這是什麼

老師自編數位教材的編輯器 MVP。老師匯入或編寫內容，學生在檢視台看到同樣的頁面。

**核心概念只有一句：內容是一條連續的流，頁是算出來的。**

資料裡沒有任何跟「第幾頁」有關的欄位，也沒有 x / y 座標。一個元件的位置只有兩個維度：
在流裡的第幾列、那一列怎麼分欄。頁由 `paginate()` 依可用空間即時切出來。

這個決定貫穿整份程式碼，往下每一條規則都是它的結果：

- 目次不存頁碼，存的是 rowId
- 「跳到某一段」不存頁碼，存的是 rowId
- 老師改字級或頁面比例，頁數當場就變，因為頁本來就是算的

## 二、先跑起來

```bash
npm install
npm run dev
```

| 指令 | 用途 |
|---|---|
| `npm run dev` | 開發伺服器 |
| `npm run typecheck` | TypeScript（`tsc -b`，含 src 與 scripts） |
| `npm run test` | 單元測試。排版引擎的正確性靠這個把關 |
| `npm run build` | 型別檢查 + 打包 |
| `npm run lint` | oxlint |
| `npm run inspect` | **引擎檢視器**。餵真教材進去，把算出來的頁印成文字 |
| `npm run coverage <md>` | 解析器涵蓋率：哪些構造被認出來、有沒有東西掉進內文 |
| `npm run tokens` | 從 `design/tokens.raw.txt` 重新產生 token |
| `npm run icons` | 從 `design/icons/` 重新產生圖示模組 |

`npm run inspect` 特別值得先跑一次：不必讀測試就看得見分頁引擎的行為。

## 三、程式碼在哪

```
src/
  model/       純資料與純函式。沒有 React，沒有 DOM
    types.ts       文件模型。欄位對應客戶的《教科書 md 標記規格》
    paginate.ts    分頁引擎。整個系統的核心
    reducer.ts     所有改動都經過這裡（純函式）
    history.ts     復原重做。因為 reducer 是純的，這是免費的副產品
    markdown.ts    md 解析
    spans.ts       行內樣式的切片與拼接
    inlineDom.ts   行內樣式 ⇄ DOM 的雙向轉換
    blockSizing.ts 非文字元件的尺寸規則（量測與渲染的唯一來源）
    outline.ts     目次（由角色算出來）
  measure/     真實量測。把元件放進隱形探針，問瀏覽器它多大
  hooks/       useEditor（狀態）、usePages（分頁）
  editor/      編輯用的東西：拖曳、浮動膠囊、Pop-up 面板、匯入、設定
  viewer/      學生看到的補充檢視器
  components/  元件的呈現
  demo/        示範用的種子資料（見第七節）
design/
  tokens.raw.txt   152 個設計 token 的來源，從 Figma 匯出
  icons/           47 個圖示的 SVG
  sample-*.md      兩份真實教材，測試會讀它們
```

**改東西之前先想它屬於哪一層。** `model/` 不可以 import React 或碰 DOM——那是它能被完整測試的原因。

## 四、真的、假的、還沒做的

這是交接時最重要的一張表。**介面都是對的，有些裡面是假的。**

### 真的（可以直接用）

- 分頁引擎：自動切頁、手動換頁線、長段落跨頁、直排橫排
- 行內編輯：注音、重點詞、注釋號在打字時都不會斷
- 拖曳重排、併欄、欄寬調整
- md 匯入（13 種構造）與匯入前的辨識報告
- Pop-up：設定、角標、學生檢視器
- 目次、頁面縮圖、教材設定
- 復原重做（含匯入）

### 假的（介面對，內容要接客戶的 API）

| 項目 | 現況 | 接上要改哪裡 |
|---|---|---|
| 圖片／影片／音檔上傳 | 存成瀏覽器的 object URL，**重新整理就沒了** | `src/editor/assets.ts` 的 `putImage` / `putFile` / `urlOf`。元件一行都不用動 |
| 網頁能否頁內嵌入 | `embeddable` 恆為 false，一律開新分頁 | 要看對方的 `X-Frame-Options` / CSP，那是伺服器端的檢查。判斷完把欄位打開就有頁內瀏覽 |
| 媒體庫 | 插入面板有入口，選了會產生一個空圖片 | 接素材庫的清單與挑選畫面 |
| AI 生成 | 插入面板底下有預留位置，**刻意標成還不能按** | 只要改那一顆的 onClick。模型與帳單在客戶端，不在這次範圍 |
| 互動模組 | 認得種類與參數、量得準、排得動，**不實作任何一種模組的內容** | 見第六節「加一種新元件」 |

### 還沒做的（刻意）

- 出版社端的內容管理
- 老師的畫筆／標註（已與客戶確認移到授課端）
- 區塊層級的 `@排版` 指令（解析時列在「刻意不處理」）
- md 的程式碼圍籬（` ``` `）：教科書規格裡沒有這個構造

## 五、五條不能破的規則

這五條每一條都是踩過坑才寫下來的。**改動時如果覺得某一條礙事，先讀它的理由。**

### 1. 量測與渲染必須共用同一個來源

非文字元件的尺寸只有 `blockSizing.ts` 一份，量測端與畫面端都呼叫它。
文字則兩邊共用 `spansToHtml()` 產生的 HTML 與 `.ds-rich` 這個 class。

兩邊各算各的，結果是量測說 240、實際畫出 626，內容被切掉，看起來像滿版出血。

### 2. 量測器不可以把結果夾在一頁之內

回傳真實尺寸就好。放不下要切開還是移到下一頁，是 `paginate` 的判斷。

夾住的話，一段比整頁還長的課文會量成「剛好一頁」，分頁器認為放得下，
整段塞進去然後溢出，而且**永遠不會觸發切頁**。

### 3. 編輯用的提示一律絕對定位，不佔版面

握把、拖曳落點線、Pop-up 角標、插入點都是這樣做的。

**而且要用實體屬性（top/left/width…）判斷方向**，不要用 `block-size` / `margin-block`
這類邏輯屬性去「抵銷」自己的尺寸——邏輯屬性算的是元素自己的書寫方向，
一旦它為了讓文字正著看而設了 `horizontal-tb`，直排時父層要的那一軸就沒被抵銷到。
插入點就是這樣在直排下每列多吃掉約 100px，橫排完全看不出來。

### 4. 識別屬性只掛在可編輯的那一份

`data-row-id` / `data-block-id` / `data-page-index` 只有編輯區會掛。

縮圖列與預覽畫的是同一個元件，兩邊都掛的話整份文件會有兩組同樣的 id，
而縮圖列在 DOM 裡排在前面，`document.querySelector` 會先找到縮圖那一份。

### 5. 沒有效果的動作要回傳原本的物件

`reducer` 裡任何「什麼都沒改」的情況都要回傳原本的 doc，不要回傳一個內容相同的新物件。
否則「把粗體設成粗體」這種操作會佔掉一次復原。

## 六、加一種新元件

客戶要的「預留擴充空間」就是這條路。步驟：

1. `model/types.ts` 的 `Block` union 加一支
2. `model/registry.ts` 加一筆（能不能切開、能不能掛 Pop-up、是不是盒狀）
3. `model/blockSizing.ts` 給它一個尺寸規則
4. `components/blocks/` 加一個呈現元件，並在 `BlockView` 的 switch 接上

**第 4 步忘了會編譯失敗**，不會等到畫面上出現空白方塊才發現——`BlockView`
的 default 分支有 `const never: never = block`。

排版、拖曳、Pop-up、復原都會自動支援，不需要動既有元件。

## 七、示範用的種子

`src/demo/seed.ts` 會在開啟時掛三個補充到第一段內文上。

**這些不在 md 裡**，是程式碼加的。因為 Pop-up 是老師編出來的，md 規格沒有這個構造，
匯入任何真實教材都不會有補充——那樣一開啟就看不到這次改版最重要的功能。

不要的話，`App.tsx` 不要呼叫 `seedDemo` 即可，其餘一行都不必動。

## 八、已知限制

- **表格的儲存格不換行**，過長會截斷。要支援換行必須讓量測器真的去量表格，
  目前是從列數乘以固定行高算的（`blockSizing.ts` 的 `TABLE_ROW`）
- **表格不能跨頁**。比一頁還高的表格會截斷，但會在表格下方明說還有幾列
  放不下（`tableOverflow`）——資料表格靜靜少了幾列，老師不會發現。
  要支援跨頁必須讓下一頁重畫表頭，否則半張表看不懂
- **md 匯入的圖片比例是估的**。md 只有檔案路徑讀不到尺寸，真實比例要在上傳時
  從 `naturalWidth / naturalHeight` 讀進來。所以未上傳、只從 md 來的圖，
  含圖頁面的分頁高度是估的
- **上傳的檔案重新整理就沒了**（見第四節）
- 設計系統目前**沒有遮罩（scrim）的 token**，檢視器與對話框的半透明背景是寫死的
- **字型目前從 Google Fonts 載入**（`index.html`）。正式上線要考慮自架：
  現在依賴 Google 的可用性，而且第一次載入前會短暫看到 fallback 字。
  注意 PingFang TC 是 Apple 系統字、不可嵌入散布，只能當 fallback

## 九、等客戶回覆才能定案

- **內容分類（`ContentCategory`）**：目前是暫定的八個值。正式清單到了只需要改
  `types.ts` 那個 union，資料結構不受影響。這是「系統讀不讀得懂教材」的關鍵欄位——
  沒有它，生字表、全書搜尋、正確順序的朗讀都做不到
- 九種文字角色的命名是否符合他們內部的說法
- 《設計指引》第十章是否存在（目前手上的版本沒有）

## 十、版權注意

`design/sample-shehui.md` 與 `design/sample-guowen.md` 是**康軒與翰林的真實課文**
（社會 U4-L1、國文 L07）。測試會直接讀這兩個檔驗證解析器涵蓋率。

**這個 repo 目前是 private。轉公開或加外部協作者之前，這兩個檔要先確認版權。**
